(** The type [direction] represents a direction either horizontally 
    or vertically. *)
type direction =
  |Up
  |Down
  |Left
  |Right

(** [play ] starts the game. *)
(* val play : unit *)

(** [game ()] prompts for the game to play, then starts it. *)
(* val game : unit -> unit *)

